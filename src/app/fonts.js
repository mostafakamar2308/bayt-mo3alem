import { Yantramanav, Archivo_Black } from "next/font/google";

export const yantramanav = Yantramanav({
  weight: ["400", "700"],
  subsets: ["latin"],
  variable: "--font-yantramanav",
});

export const archivo_black = Archivo_Black({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-archivo_black",
});
